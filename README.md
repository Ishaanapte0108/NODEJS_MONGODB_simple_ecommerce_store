install all node modules using npm init

make a .env file create a constant called DB_CONNECTION in it and equalize it to your mongodb connection string (you may have to install a processes module)

GET STARTED!

